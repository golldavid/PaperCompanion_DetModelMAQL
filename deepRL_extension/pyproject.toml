[project]
name = "deeprl-extension"
version = "0.1.0"
description = "Deep RL extension for multi-agent learning dynamics research"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.0",
    "numpy>=2.0",
    "matplotlib>=3.8",
    "jupyter",
    "ipykernel",
    "scipy>=1.15.3",
]

[tool.uv]
dev-dependencies = []
